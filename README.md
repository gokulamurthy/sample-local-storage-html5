This is a sample code for jquery ui dialog boxes with local storage data usage
of html5.

To clear the local storage data explicitly use the below code under
$(document).ready() method:

localStorage.clear();


